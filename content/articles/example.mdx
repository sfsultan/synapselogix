---
title: Example MDX Article Changed
description: A brief overview of the most in-demand programming languages you should consider learning in 2023 to stay ahead of the curve in the tech industry.
status: draft
published: 2023-09-22
keywords: Angular, React, Dackdon
featuredImage: /articles/image-1@2x.jpg
---

import { Tip } from "@/components/tip"
import { Backmatter } from "@/components/backmatter"
import { Card } from "@/components/ui/card"

<Tip 
  title="Explaining the Regex 🗿"
  intro="@radix-ui/primitives"
  description="@radix-ui/colors"
/>

> Layers. Onions layers. Ogres have layers. Onions have layers. You get it? We both have layers.
<Card href="#">
 
#### Heading
 
You can use **markdown** inside cards.
 
</Card>
# Overview

This page is just an example MDX. 

## TOC

Will be auto-generated from h2 headings

[Text](url)
![alt text](path-to-image)
## MDX blocks

### Paragraph

Lorem ipsum, or  lipsum as it is sometimes known, is dummy text used in laying out print, graphic or web designs. The passage is attributed to an unknown typesetter in the 15th century who is thought to have scrambled parts of Cicero's De Finibus Bonorum et Malorum for use in a type specimen book.
H~2~O
***


## Autolink literals

www.example.com, https://example.com, and contact@example.com.

## Strikethrough

~one~ or ~~two~~ tildes.

## Tasklist

* [ ] to do
* [x] done

asdasdasdasd

### List
  - one 
  - two
  - three

### Code blocks

```js
// index.html
// --------------------
async function main() {
  const allUsers = await prisma.user.findMany()
  console.log(allUsers)
}
```

```prisma
datasource db {
  provider = "sqlite"
  url      = env("DATABASE_URL")
}
generator client {
  provider = "prisma-client-js"
}
model Post {
  id        Int     @id @default(autoincrement())
  title     String
  content   String?
  published Boolean @default(false)
  author    User?   @relation(fields: [authorId], references: [id])
  authorId  Int?
}
model User {
  id    Int     @id @default(autoincrement())
  email String  @unique
  name  String?
  posts Post[]
}
```

### Code block with copy

### Code block with line number

```js line-number
async function main() {
  const allUsers = await prisma.user.findMany()
  console.log(allUsers)
}
```

### Code block with highlighted code

```js 
async function main() {
+  added code
-  deleted code
|  neutral highlight
}
```

### Inline code

### File name with icon

4 different icons available

### Tabbed blocks


### Expand/Collapse section


### Expand/Collapse section


### Fancy button

  Button text
# Onions

Layers. Onions have layers. Ogres have layers. Onions have layers. You get it? We both have layers.

Oh, you both have layers. Oh. You know, not everybody like onions.

```ts:lib/mdx.ts
const year = "2001";
const movie = "Shrek";

console.log(`${movie}, ${year}`);
```
## History
```js {1,3-4} showLineNumbers
function fancyAlert(arg) {
  if (arg) {
    $.facebox({ div: '#foo' })
  }
}
```


```html {1,3-4} showLineNumbers
<code class="language-js">
  <div class="code-line line-number highlight-line" line="1">
    <span class="keyword">function</span>
    <span class="function">fancyAlert</span><span class="punctuation">(</span
    ><span class="">arg</span><span class="punctuation">)</span>
    <span class="punctuation">{</span>
  </div>
  <div class="code-line line-number highlight-line" line="2">
    <span class="keyword">if</span>
    <span class="punctuation">(</span>arg<span class="punctuation">)</span>
    <span class="punctuation">{</span>
  </div>
  <div class="code-line line-number" line="3">
    $<span class="punctuation">.</span><span class="function">facebox</span
    ><span class="punctuation">(</span><span class="punctuation">{</span> div<span class="">:</span>
    <span class="string">'#foo'</span>
    <span class="punctuation">}</span><span class="punctuation">)</span>
  </div>
  <div class="code-line line-number" line="4">
    <span class="punctuation">}</span>
  </div>
  <div class="code-line line-number" line="5">
    <span class="punctuation">}</span>
  </div></code
>
```

```js /age/#v /name/#v /setAge/#s /setName/#s /50/#i /'Taylor'/#i
const [age, setAge] = useState(50);
const [name, setName] = useState('Taylor');
```